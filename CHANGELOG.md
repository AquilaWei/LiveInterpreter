# 變更紀錄

版本號是 `大.中.小`，唯一的來源是根目錄 `Cargo.toml` 的 `[workspace.package] version`。

| 位 | 什麼時候進 |
|---|---|
| 大 | 由維護者決定 |
| 中 | 新功能落地，**而且正確性已經驗證過** |
| 小 | 修 bug；或功能已寫完、還沒驗證完 |

中版號與小版號的分界是「驗證過沒有」，不是「算不算新功能」。看到 `x.y.0`，意思是那個
功能被量過、確認過，不只是寫完了。

回報問題時請附上 `liveinterpreter --version` 的輸出（或設定視窗最下面那一行），例如
`LiveInterpreter 1.3.2 (a1b2c3d4 2026-09-18)`。後面帶 `modified` 表示那個 build 來自
有未提交改動的工作區。

---

## 1.3.9 — 2026-09-24

**修正：翻譯常冒出跟原文無關的句子、翻到一半就停、或少一個字變成別的詞。** 這一版修了翻譯
的三個問題：

- **少冒出無關的句子、少翻一半。** 翻譯模型改成先翻成簡體中文，再轉成台灣繁體。它的繁體
  訓練資料比較少也比較雜，直接翻繁體時，遇到短句常會吐出網頁上的固定字句（「您的位置：
  首頁」「沒有任何問題」「沒有任何樓盤符合您的搜尋結果」），長句也比較容易只翻前半。一段
  57 分鐘的會議裡，這類亂句從 38 行降到 0 行；翻到一半就停的句子大約少了一半到三分之二。
- **停頓不再被翻成句子。** 辨識把停頓寫成「. . .」，以前每個點都被當成一句話翻譯，變成一整
  排「沒有人知道。沒有人知道。……」（同一段會議有 122 次）。現在停頓就是「……」。
- **不再少字。** 翻譯模型的字庫缺少幾個字（饋、碩、鋰等），以前這些字會被直接吃掉，
  「回饋」變成「反」、「法學碩士」變成「法學士」，甚至變成「你計程車」。現在遇到這種情況，
  那一小段改由第二個翻譯模型（Opus-MT）翻；兩個都寫不出來時，缺字的位置會顯示「□」，不再
  默默消失。

其他：

- 第一次啟動或按「下載模型」時會多下載約 82 MB 的 Opus-MT。沒有下載它，程式仍然正常運作，
  只是缺字會顯示為「□」。
- 翻譯每句平均慢約 0.05–0.1 秒；語音轉檔一段 57 分鐘的會議從約 7.8 分鐘變成約 8.8 分鐘。
- Opus-MT 是 CC-BY-4.0 授權，可商用，出處已列在 NOTICE。
- 已知仍會發生：短句偶爾翻得怪（例如「Bye.」→「別見你」）、看不懂上下文的詞仍會選錯意思
  （例如 envelope 翻成「信封」）。這是翻譯模型本身的限制。

## 1.3.8 — 2026-09-23

**修正：語音轉檔選「中英對照」或「只要中文」時，英文逐字稿後半段被一再重複的幻覺中文蓋掉。**
例如一段 7 分鐘的錄音，從 1:13 起幾分鐘都是「但是美沒有完美沒有完」之類的句子，真正講的
內容不見了。原因是轉檔時會把「前面聽到的內容」提示給語音辨識，好讓專有名詞與標點前後一致，
但提示裡誤帶了已經翻好的中文；辨識模型以為對方在講中文，便照抄起來，一句錯了下一句跟著錯。
現在只提示英文原文。

- 同一份錄音不論選哪種輸出，英文內容都一樣了。
- 一段 24 分鐘的對話：英文行裡的幻覺中文從 71 行降到 0 行，辨識出的英文多了約三成。
- 轉檔也變快：那 24 分鐘的對話從約 7 分鐘縮短到約 3 分鐘。
- 已知仍會發生：講話含糊、聽不清楚的地方，偶爾會出現一長串重複的「Yeah. Yeah. Yeah.」。

## 1.3.7 — 2026-09-23

**修正：「Mm-hmm.」「Oh.」「Right?」這類語助詞被翻成「沒有任何問題」。** 翻譯模型遇到
沒有實際內容的短句會自己編一句話出來，讓說話的人「說了」他沒說過的話。現在整句都是語助詞
時不再交給模型，直接換成對應的中文：嗯、呃、喔、啊、對、好……標點照原樣保留。

- 各種拼法都認得：Um、Umm、Uhm、Mm-hmm、Mhm、Hmmm、Ohhh、Yeahhh 等。
- 只要句子裡有一個實際的詞（例如「Yeah, so...」），就照常翻譯，不會吞掉內容。
- 即時字幕與語音轉檔都適用。一段 24 分鐘的對話裡，這類錯誤從 18 處降到 1 處；剩下那處是
  模型把有內容的句子翻錯，不屬於語助詞。

## 1.3.6 — 2026-09-23

語音轉檔的兩個修正，都是用一段 24 分鐘的真實錄音試出來的：

- **修正：某些錄音轉檔到一半就失敗**（「the transcription thread panicked」）。一句話
  剛好講到接近 12 秒上限、後面緊接著停頓時會觸發。
- **修正：錄音裡夾雜中文時，逐字稿出現一整串「��是����」亂碼。** 英文模型聽到中文會照寫
  中文，但一個中文字常被切在兩段之間，原本兩半各自解碼就壞了。即時字幕也有同樣的問題，
  一併修好。

## 1.3.5 — 2026-09-23

語音轉檔的兩個小修正，都是實際操作命令列時發現的：

- **模型沒下載時立刻告訴你**：原本要先把整個音檔解碼完才報錯，一小時的檔案要白等。
- **命令列按 Ctrl-C 後，「stopping…」不會再被進度數字蓋掉。**

## 1.3.4 — 2026-09-23

**修正：在設定視窗切換音訊來源沒有作用。** 存檔後字幕會重新啟動（模型重新載入、開新的
逐字稿），看起來像是切換了，其實重新打開的還是**原本的來源**；要把程式整個關掉再開，
新的來源才會生效。2026-09-22 的實際紀錄裡就是這樣：切到麥克風後，重新打開的仍然是喇叭
的聲音。

原因是引擎一直拿著程式啟動時讀到的那份設定。現在每次重新啟動都會先讀取最新存檔的設定，
所以轉檔期間切換的來源，也會在轉檔結束、字幕恢復時生效。

## 1.3.3 — 2026-09-23

**新功能（測試版）：語音轉檔。** 選一個英文音檔，拿到一份文字檔，內容可選英文、中文或
中英對照。小版號而不是中版號，因為桌面版的視窗還沒在實機上點過一遍。

- **讀得懂常見格式**：mp3、m4a、flac、ogg、wav，mp4 影片裡的音軌也可以。不需要 ffmpeg。
- **每一句都走精準線、每一句都翻譯**。即時字幕為了跟上講話速度會捨棄一些東西，檔案不必趕，
  所以一句都不丟。在 Intel Arc 上，100 秒的會議錄音約 30 秒轉完。
- **一口氣講很久的人不會被切在字中間**：超過 12 秒沒停頓時，往回找最安靜的一刻切開，
  切口兩邊不重疊，不會有同一個詞出現兩次。
- **桌面版**：設定視窗 →「開啟語音轉檔…」，有進度條、可取消；轉檔時即時字幕暫停，
  結束後自動恢復。**命令列**：`liveinterpreter --transcribe 檔案 --output en|zh|both`。
- 檔案寫到逐字稿資料夾，檔名沿用音訊檔名，同名時加 `-2`、`-3`，不覆蓋；取消或失敗
  不會留下半個檔案。

**修正：兩個 whisper 模型同時載入到 GPU 時程式會當掉。** 這是 whisper.cpp 的 Vulkan
後端的問題；即時字幕只用一個模型，所以以前碰不到，平行測試才把它翻出來。現在同一時間
只載入一個。

## 1.3.2 — 2026-09-18

第一個公開版本。程式行為與 1.3.1 相同。

- **CI 能在沒有 GPU、沒有模型的機器上跑**：只檢查邏輯（fmt、clippy、全部測試），
  需要模型的測試會印出 SKIP 並通過。原本有一個測試在沒有模型時會直接失敗。
- 程式碼註解改成自足的說明，不再指向 repo 外的內部文件。

## 1.3.1 — 2026-09-18

**修正：下載視窗的進度條不會動。** 下載視窗沒有列在 Tauri 的權限設定裡，所以它的
前端無法訂閱事件，初始化在第一行就中斷：進度條、模型清單、總大小都不顯示，「稍後
再說」也按不動。下載本身照常完成，所以這個錯誤完全不會出聲。

新增一個測試，任何視窗漏列權限時都會失敗 —— 這是同一種錯誤第二次發生。

## 1.3.0 — 2026-09-18

**Flatpak 成為正式的發行方式。** 程式碼與 1.2.4 相同，這一版記錄的是「驗證完成」：

- 在開發機上完整跑過：建置、安裝、下載 1 GB 模型並驗 sha256、Vulkan 用到真的 GPU、
  三個全域快捷鍵、點擊穿透、逐字稿寫進主機的 `~/Documents`。
- 在乾淨的 Debian 13 容器裡從零安裝成功（沒有 flatpak remote、沒有 runtime、沒有中文
  字型），並用負向對照確認這個測試真的測得出失敗。

還沒驗的：另一台實體電腦上的畫面、音訊與 GPU。

## 1.2.4 — 2026-09-17

修兩個只在「別人的電腦」上才會出現的安裝問題：

- **Flatpak 找不到 runtime。** 安裝包現在帶著 Flathub 的位址，沒裝過 GNOME runtime 的
  機器會自動加上 Flathub 並下載相依。
- **沒有中文字型的系統上，譯文是一排空格子。** GNOME runtime 不含任何中文字型，原本是
  借用主機的。現在內附 Noto Sans TC（5.4 MB 的子集版，拿實際譯文比對過，涵蓋率
  99.996%）。授權 OFL-1.1，見 `NOTICE`。

## 1.2.3 — 2026-09-17

**修正：Flatpak 回報的版本號錯誤。** AppStream metainfo 裡的版本清單漏更新，所以
`flatpak list` 顯示的版本與程式自己報的不同。建置腳本現在會比對兩者，不一致就停止
建置。

## 1.2.2 — 2026-09-17

**新增 Flatpak 打包**（app id `io.github.AquilaWei.LiveInterpreter`）。原因是 rpm/deb
裡的執行檔要求 glibc ≥ 2.43，只能裝在很新的系統上；Flatpak 自帶 runtime，不受這個限制。

- `packaging/flatpak/` 與 `scripts/flatpak.sh`。主機不需要安裝 flatpak-builder。
- **修正：在 Flatpak 裡下載的模型，程式一結束就消失。** 沙箱裡的 `$HOME` 是暫存的，
  模型快取現在跟設定檔一樣依 `XDG_CACHE_HOME` 決定位置。
- **修正：GPU 索引錯位。** 過濾掉軟體光柵器之後，回報給 whisper.cpp 的裝置編號可能
  指到別顆 GPU。
- 精準線明確拒絕軟體光柵器（`llvmpipe`），寧可退到 CPU 模型，也不要看起來在用 GPU
  實際上在 CPU 上慢慢跑。

## 1.2.1 — 2026-09-17

只有文件，程式碼沒有變動。實測確認 Flatpak 沙箱裡系統聲音、麥克風與 Vulkan 都能用，
並發現現有的 rpm/deb 只能裝在 glibc ≥ 2.43 的系統上 —— 這是改做 Flatpak 的理由。
Windows 與 Android 暫緩。

## 1.2.0 — 2026-09-14

**模型自己下載。** 桌面版第一次啟動時如果模型不齊，會開下載視窗；命令列用
`liveinterpreter --fetch-models`。共約 1 GB。

- 先寫成 `.part`，sha256 驗過才改名就位；中斷可以續傳；雜湊不符就刪掉並報錯。
- 雜湊來自實際量測 WER 時用的那一份模型，所以下載到的保證是同一份。
- sherpa 的模型逐檔下載，只抓需要的 int8 檔，比下載整包少 300 MB。

補上授權：程式碼 Apache-2.0（`LICENSE`），第三方素材的授權列在 `NOTICE`。

## 1.1.0 — 2026-09-11

**句中提早翻譯。** 講得快、中間不停頓時，一行會拖很久才收尾，中文也跟著晚出現。現在
快線只要在行中間認出一個穩定的句號，就先把那一段送去翻譯並顯示；行不會被切開，逐字稿
不受影響。實測在講得快的片段上，中文提早 5–10 秒出現，定稿翻譯沒有變慢。可以在
`config.toml` 用 `[mt] draft_mid_line = false` 關掉。

## 1.0.0 — 2026-09-11

第一個穩定版本：在 Linux 上自用已經穩定。

- 雙線辨識：快線先把字打上螢幕，精準線隨後原地覆蓋成更準的版本。
- 英翻繁中（台灣用詞）、懸浮字幕條、設定視窗、逐字稿（txt / srt / vtt / jsonl）、
  rpm 與 deb 安裝包。
- 快線加上標點與大小寫：原文列不再是全大寫、沒有標點。
- 翻譯改成依標點的來源切句，長句的後半不再被吞掉。
- 執行檔會回報自己是哪一個 build。
- 修正：講太快時一行可能拖到 20 秒才收（上限改為 12 秒）。
- 修正：在設定視窗切換音訊來源不會生效。
